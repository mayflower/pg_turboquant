# third_party

This directory is reserved for pinned source dependencies.

Expected future contents:

- `pgvector/` — pinned pgvector source checkout or submodule used to provide a stable build and compatibility target for `vector` and `halfvec`.

Do not assume system-installed pgvector headers are sufficient for development until the repository proves otherwise.
