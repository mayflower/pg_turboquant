This directory contains the current extension install script.

- `pg_turboquant--0.1.0.sql` is the file used by `CREATE EXTENSION pg_turboquant`
- it defines the SQL-visible surface shipped in this release
